/**
 * The name of the person.
 * @type {string}
 */
const country = "Mexico";

/**
 * Colors array
 * 
 * @type {Array<String>}
 */
const colors = ["red", "blue", "yellow"];

/**
 * Current ages
 * 
 * @type {Array<Number>}
 */
const ages = [22, 15, 24, 42];

/**
 * Mixed array with numbers and strings
 * 
 * @type {Array<String|Number>}
 */
const mixed = [1, "a", 2, "b", "c", 3];

/**
 * Person Object
 * @type {{
 *  id: number,
 *  name: string,
 *  age: number,
 *  country: string|null,
 *  active: boolean
 * }}
 */
const estudiante = {
    id: 1,
    name: "Daniel",
    age: 25,
    country: null,
    active: false,
};
